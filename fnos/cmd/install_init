#!/bin/bash
# cmd/install_init —— 安装前检查
#
# 只做安装包自身的静态校验（镜像占位符）。
#
# 注意：这里【不要】用 docker info 预检 Docker！在飞牛上，容器编排文件的
# 实际的创建/启动由应用中心（以 root 跑自己的 docker daemon）负责，install_init
# 只是安装前的一个 hook。若在这一步检查 docker，反而会在应用中心的执行上下文里
# 误报「Docker 服务未启动或当前用户没有访问权限」——即使 Docker 明明正常运行。
# （参考：飞牛应用商店里的 Docker 应用 install_init 普遍只 exit 0。) 因此这里
# 只保留与 Docker 无关的镜像占位符校验。

COMPOSE_FILE="${TRIM_APPDEST}/docker/docker-compose.yaml"
if [ -f "$COMPOSE_FILE" ] && grep -q 'image:[[:space:]]*yourname/' "$COMPOSE_FILE"; then
    echo "这个安装包还没有指定容器镜像（编排文件里的 image 仍是 yourname/... 占位）。<br>请开发者先运行 docker/build-and-push.sh 推送镜像，并把 fnos/app/docker/docker-compose.yaml 里的镜像地址改成自己的，再重新打包。" > "${TRIM_TEMP_LOGFILE}"
    exit 1
fi

exit 0
