# 安装时由 cmd/install_callback 按机器实际情况重写（指向应用的 var 目录）；
# 这里只是兜底默认值，保证即使回调脚本失败应用也能起来。
# 镜像地址不在这里配——它写在 docker-compose.yaml 里，打包前必须改掉。
VS_DATA=/vol1/@appdata/video-splitter
VS_EXPOSE_PORT=8099
TZ=Asia/Shanghai
PUID=1000
PGID=1000
